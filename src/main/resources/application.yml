server:
  port: 8080

spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1521:xe
    username: c##scott
    password: tiger
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
#    database-platform: org.hibernate.dialect.Oracle12cDialect
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: INFO
    com.yourname.prodservice: DEBUG

